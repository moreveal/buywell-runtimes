# GGSel on Buywell Edge

Install the package and create a `ggsel.seller` connection with the seller ID
and a local Seller API V1 key. The separate GGSel app, service scripts, and a
Buywell connection key are no longer required.

Events, actions, and the catalog remain compatible with
`ggsel.seller@1.2.7`.
