"""Playerok event listener package.

The upstream commit ships this directory without a package marker, which
causes standard wheel builds to omit it. Keeping the marker here is the only
packaging delta from the pinned upstream source.
"""
